<h2> Seja bem vindo a timeline</h2>

<div class="row">
  <div class= "offset-2 col-lg-8">
    <%= @posts.each do |post|%>
      <%= render partial: "user/post_card", locals: { post: post } %>
    <% end %>
  </div>
</div>